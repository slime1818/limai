# Fase 2 start. Scroll architecture, 6 biomes.

## Current state

Fase 1 COMPLETE per commit f135563. Apu hero live op:

- limai-one.vercel.app, werkend direct op Vercel subdomain.
- limaidesign.com, DNS propagation gestart 2026-04-24, beschikbaar binnen 24 tot 48 uur.

Runtime stand:

- Next.js 16.2.4 App Router, TypeScript, Tailwind v4 met `@theme` tokens voor LimAI palette plus per-biome accenten (apu, puna, yungas, selva, paracas, pacifico) in `app/globals.css`.
- next/font Fraunces (met opsz axis) plus Inter, CSS-vars via `app/layout.tsx`.
- `app/page.tsx` rendert één `<BiomeSection>` voor Apu met image, F6 scrim (horizontal desktop, vertical mobile), titel plus tagline. Body background `noche-andina`, flex-col layout.
- `public/biomes/apu.webp` is de enige runtime composite (apu-composite-alt variant per commit 489f57b). De 5 andere biomes wachten in `public/Backdrops/[biome]/processed/[biome]-composite.webp`.
- Geen scroll-mechanica, geen routing buiten `/`, geen dots, geen Lenis initialisatie, geen footer.
- Dependencies voor Fase 2 zijn aanwezig in package.json: `lenis@1.3.23`, `gsap@3.15.0`, `motion@12.38.0`, plus `@react-three/fiber@9.6.0`, `@react-three/drei@10.7.7`, `@14islands/r3f-scroll-rig@8.15.0`, `three@0.184.0` voor Fase 3 plus 4.

## Fase 2 objective

6-biome homepage scroll werkend op Vercel. Concreet:

- Biome component pattern rendert 2 viewports per biome. Viewport 1 is hero (titel, tagline, CTA), viewport 2 is content-teaser (korte preview-copy, geen full agency content).
- Altitude-pan per biome tijdens scroll. Image blijft pinned via sticky, verticale transform gekoppeld aan in-section scroll-progress simuleert camera-descent.
- Seamless transitions T1, T2, T3, T5 via alpha-driven crossfade plus parallax tussen aangrenzende biome images.
- T4 (Selva naar Paracas) krijgt simple-crossfade placeholder conform T1-T2-T3-T5. Authored drama (leaves naar golden-dust metamorphose, R3F plus GSAP particle timeline) deferred naar Fase 3 per DESIGN.md handoff.
- Scroll-progress dots vaste rechterkant, 6 dots. Active dot krijgt per-biome accent kleur plus size-bump, click-to-jump via Lenis `scrollTo`.
- Subtle footer na Pacifico met KvK, copyright, cookie link, privacy link, socials. LimAI palette, minimalistisch. Link targets zijn stubs in Fase 2, worden in Fase 5 launch prep actief gemaakt.
- Demo-bar moment: deploy op limai-one.vercel.app met 6 biomes volledig scrollbaar. Niet particles (Fase 4), niet subpages (Fase 5).

## Approach

Vertical slice met Puna als pilot.

Rationale Puna als eerste scroll-target:

- 2e biome in de altitude descent, natural overgang vanaf de bestaande Apu hero.
- Eerste scroll vanuit de live-page is de meest representatieve test. Als Apu naar Puna werkt op alpha-crossfade plus altitude-pan, dan is het pattern bewezen en kunnen de 4 volgende biomes copy-paste worden.
- Valideert in één slice alle kern-mechanica: 2-viewport structuur, altitude-pan binnen sticky section, seamless transition T1, dots-activatie op minimaal 2 dots.

Volgorde binnen Fase 2:

1. Shared infrastructure eerst, geen biome-specifiek werk.
2. Puna pilot compleet, alle mechanica bewezen op één biome-paar.
3. Scale naar Yungas, Selva, Paracas, Pacifico.
4. Dots-interactivity, footer, deploy-verify.

Apu blijft visueel zoals live staat tot substep 6, waarin BiomeSection wordt gerefactored naar de 2-viewport structuur. Apu viewport 1 blijft byte-gelijk in output, viewport 2 krijgt content-teaser layout met nieuwe copy.

## Substeps Fase 2

### A. Shared infrastructure

1. **Asset intake voor 5 biomes**. Visual-verify kandidaat-composites per biome volgens visual_read_calibration workflow. Voor Puna, Yungas, Selva, Paracas, Pacifico is er alleen `[biome]-composite.webp` als enige kandidaat, geen alt-variant zoals Apu had. Viewer-check voor elke biome, dan kopie naar `public/biomes/[biome].webp`.

2. **`app/data/biomes.ts`** met typed biome-config array. Velden: `id`, `accentColor`, `image`, `imageAlt`, `title`, `tagline`, `ctaLabel`, `ctaHref` (placeholder tot Fase 5), `subpage`. Single source of truth voor de 6 biomes.

3. **`app/components/SmoothScrollProvider.tsx`** met Lenis init. Client component, wraps children, wire in `app/layout.tsx`. Timing values (duration, easing) te bepalen in deze substep zelf na een quick-test in browser. Smooth-scroll mag de `useScroll` progress-tracking niet breken.

4. **`app/components/BiomeScroller.tsx`** orchestrator. Mapt de biome-config op stacked `<BiomeSection>` instances plus transition-layers ertussen. Vervangt de losse BiomeSection call in `app/page.tsx`.

5. **`app/components/ScrollProgressDots.tsx`** fixed-right nav met 6 stubs, per-biome accent. Stap 1 is static render, niet-interactief en zonder active-tracking. Active-state komt in substep 10, click-to-jump in substep 17.

### B. Puna pilot

6. **Refactor BiomeSection naar 2-viewport structuur**. Section height wordt 200vh, image blijft pinned via sticky positioning of equivalente approach. Viewport 1 toont bestaande hero (titel plus tagline), viewport 2 toont teaser-layout (korte copy plus CTA).

7. **Puna composite visual-verify plus intake** naar `public/biomes/puna.webp`. Diagnostic sample op composite voor palette-continuity check met Apu onderkant (border-zone RGB anchors).

8. **Altitude-pan mechanic** op BiomeSection. Image verticaal transform gekoppeld aan in-section scroll-progress via `motion` `useScroll` plus `useTransform`. Start-range 8 tot 15 procent translateY, adjust na visual test op Puna.

9. **T1 seamless transition Apu naar Puna**. Alpha-crossfade plus parallax tussen Apu- en Puna-images op de biome-grens. Opacity tween gedreven door scroll-progress rond de overgangszone.

10. **Scroll-dot active-state logic**. In-view biome krijgt accent-kleur plus size-bump, andere dots blijven neutral grey. Detectie via `useScroll` op elke section ref.

11. **Mobile-responsive Puna check**. Vertical scrim-variant, type, altitude-pan gedrag, en dot-positie evalueren op aspect-ratio 0.46. Go of no-go voor Phase C scale.

### C. Scale naar resterende 4 biomes

12. **Yungas intake, content, T2 crossfade**. Pattern-replicatie van Puna.

13. **Selva intake, content, T3 crossfade**. Note: Selva gebruikt TOP framing per DESIGN.md sectie 3, scrim-variant beslissing (rotated gradient, adapted direction, of scrim-skip) wordt hier geland.

14. **Paracas intake en content**.

15. **T4 placeholder**. Simple crossfade matching T1-T2-T3-T5. Gedocumenteerd in code-comment als `placeholder, authored drama deferred naar Fase 3` zodat toekomstige Claude of Olivier het markeer-punt ziet.

16. **Pacifico intake, content, T5 crossfade**.

### D. Interactivity plus integration

17. **Scroll-dot click-to-jump**. Click handler roept `lenis.scrollTo(targetSectionTop)` met smooth behavior.

18. **`app/components/Footer.tsx`** subtle na Pacifico. KvK, copyright, cookie link, privacy link, socials. Link targets zijn placeholders in Fase 2.

19. **Cross-biome screenshot pass** op desktop plus mobile. Vergelijk met Fase 1 baseline voor Apu-regressie-check. Playwright hash-check op het live biomes-pad voor consistency.

20. **Vercel deploy plus live-verify** op limai-one.vercel.app plus (DNS gecheckt) limaidesign.com.

Totaal 20 substeps. Estimate 2 weken part-time per DESIGN.md Phase 3.

## DESIGN.md flags te verifieren voor we code schrijven

### Niet-meer-actueel (te verifieren tijdens substep A)

Geen bekende items. Fase 1 heeft de DESIGN.md tabellen plus Phase 3 scope niet invalidated.

### Missing of to-be-decided in Fase 2 scope

- **"α uniform-driven" implementatie**. DESIGN.md sectie 2 gebruikt shader-taal. Interpreteren als WebGL alpha uniform via r3f-scroll-rig `ScrollScene` (matcht Fase 4 particle-migratie), of als DOM-opacity tween via motion (lichter, sneller naar demo). Default recommendation staat in Open questions. Dit is de belangrijkste architectuur-beslissing voor Fase 2.
- **Selva TOP-framing scrim-variant**. DESIGN.md flags dat F6 left-gradient mogelijk geroteerd of adapted moet voor Selva. Concreet te beslissen in substep 13.
- **Viewport-2 teaser-copy per biome**. DESIGN.md geeft titel plus tagline plus CTA voor viewport 1, niet voor viewport 2. Placeholder-copy in Fase 2, echte teaser-content in Fase 5 of apart content-werk.
- **Altitude-pan range**. "longer/immersive variant" is kwalitatief, niet kwantitatief. Proposed starting range 8 tot 15 procent translateY op Puna, lock voor Phase C na test.
- **Mobile scroll-dots**. DESIGN.md specificeert rechterkant voor desktop. Mobile nav niet gespecificeerd. Voorstel staat in Open questions.

### Deferred naar Fase 3 of later

- T4 authored drama (leaves naar golden-dust particle metamorphose, R3F plus GSAP timeline). Fase 3 scope.
- Particles per biome (snowflakes Apu, wind Puna, rain Yungas, leaves Selva, stochastic sand Paracas, spray Pacifico). Fase 4 scope.
- Accessibility pass, prefers-reduced-motion fallback. Fase 4 scope.
- Apu widescreen composite-variant voor wider-dan-4:3 viewports. Pre-launch polish.
- CTA-links naar subpages (/over-ons, /diensten, /cases, /proces, /contact, /coming-soon). Fase 5 launch prep.

## Open questions

1. **Transition implementation architectuur**. DOM-opacity tween via `motion` `useScroll` (pragmatisch, snel, voor Fase 2 recommend) of WebGL alpha uniform via `@14islands/r3f-scroll-rig` `ScrollScene` (spec-matchend aan DESIGN.md taal, zwaarder opzet, aligned met Fase 4 particle-migratie). **Default recommendation: DOM-opacity via motion nu**. Motivatie: time-to-demo lager, `motion` is React-native en simpel, en de Fase 4 R3F-migratie kan particles plus crossfade tegelijk heroverwegen. Als dit keuzepunt anders landt, verandert Phase A (SmoothScrollProvider krijgt R3F Canvas wrapper, BiomeScroller rendert scenes in plaats van DOM sections). Beslissing voor code starts.

2. **Lenis timing values**. Defaults `duration 1.2s` plus `easing easeOutExpo` zijn redelijk startpunt. Te valideren in substep 3. Key check: smooth-scroll breekt `useScroll` progress-tracking niet, opacity plus transform moeten frame-accurate volgen.

3. **Altitude-pan range**. Start 8 tot 15 procent translateY op image binnen sticky section. Evaluatie op Puna (substep 8), lock voor Phase C scale. Te groot wordt letterbox-banden zichtbaar, te klein wordt het niet voelbaar.

4. **Footer scope in Fase 2**. Voorstel is subtle stub met placeholder KvK-nummer, placeholder cookie en privacy links, geen echte social-URLs. Full content in Fase 5.

5. **T4 placeholder style**. Simple crossfade zoals T1-T3-T5 plus code-comment marker. Alternatief is visible sentinel (tekst `T4 drama, Fase 3`) maar dat breekt de demo-look. Recommend simple crossfade, duidelijk gemarkeerd in code.

6. **Mobile scroll-dots**. Voorstel: hide op sub-md breakpoint voor Fase 2, revisit als onderdeel van mobile-nav strategy in Fase 5. Alternatief is reposition naar bottom-center, of kleinere variant rechts. Hide is het goedkoopst en voorkomt overlap met content in smalle viewports.

7. **Viewport-2 CTA styling**. DESIGN.md sectie 3 zegt "A style: minimalist underlined text plus arrow" voor homepage biome-CTAs. Te implementeren in BiomeSection viewport-2 layout, consistent cross-biome.

## Repo impact

### Nieuwe files

```
app/
├── components/
│   ├── SmoothScrollProvider.tsx    ← Lenis wrapper, client component
│   ├── BiomeScroller.tsx           ← orchestrator, mapt biome-config op stacked sections plus transities
│   ├── ScrollProgressDots.tsx      ← fixed right-nav, 6 dots, per-biome accent, click-to-jump
│   └── Footer.tsx                  ← subtle footer na Pacifico
└── data/
    └── biomes.ts                   ← typed biome-config, single source of truth

public/biomes/
├── puna.webp                       ← uit substep 1 intake
├── yungas.webp
├── selva.webp
├── paracas.webp
└── pacifico.webp
```

### Gewijzigde files

- `app/components/BiomeSection.tsx`. Refactor naar 2-viewport layout plus altitude-pan plus scroll-progress reporting. Viewport 1 blijft byte-gelijk voor Apu output, viewport 2 krijgt nieuwe teaser-layout. Substeps 6, 8.
- `app/page.tsx`. Swap `<BiomeSection>` call voor `<BiomeScroller biomes={biomes} />`. Substep 4.
- `app/layout.tsx`. Wrap children in `<SmoothScrollProvider>`. Substep 3.
- `app/globals.css`. Mogelijk scroll-container utilities of scrollbar-hide reset, decide tijdens substep 3.

### Nieuwe folders

- `app/data/` voor biome-config (eerste data-folder in de repo).

### Geen nieuwe dependencies

Alle libraries voor Fase 2 scope (`lenis`, `gsap`, `motion`) zitten al in package.json uit de Fase 1 scaffold (commit 9ab4bba). R3F-stack zit ook al klaar voor Fase 3 plus 4.

## Immediate next action

Review van deze status doc door Olivier. Focus op:

1. De architectuur-keuze in Open question 1 (DOM-opacity vs WebGL-alpha-uniform). Dit fork de uitvoering van Phase A.
2. Of Puna als pilot klopt, of een andere keuze beter is (alternatief zou Pacifico kunnen zijn om T5 als laatste transition vroeg te testen, maar dat breekt de natural altitude-descent validatie).
3. Of de 20-substep granulariteit de goede maat is, of te fijn of te grof.

Na approval:

1. Commit plus push deze status doc als eerste Fase 2 commit.
2. Start met substep 1, asset intake voor de 5 resterende biomes volgens visual_read_calibration workflow (visual verify kandidaat composites voor copy naar `public/biomes/`).

Niet meteen code. Eerst alignment op de transition-architectuur keuze, daarna de intake-run.
