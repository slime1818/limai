# Fase 1 start — Next.js scaffolding

## Current state
LimAI art-gen phase complete (6/6 biomes production-locked, commit c9fce42). All assets live in public/Backdrops/[biome]/. Cross-biome viewer validates composites. GitHub repo synced at https://github.com/slime1818/limai (branch main).

## Fase 1 objective
Next.js 14 App Router scaffolding in site/ subfolder of existing repo. Deliverable: localhost:3000 shows Apu backdrop fullscreen with title "LimAI" + tagline, correct typography (Fraunces+Inter), correct palette, F6 scrim as CSS overlay. No scroll-mechanica yet.

## Approach
Vertical slice — Apu volledig werkend voordat Puna, etc. Demo-bar op elk moment.

## Substeps Fase 1
1. npx create-next-app@latest in site/ (TypeScript + Tailwind + App Router)
2. next/font setup Fraunces + Inter
3. tailwind.config.ts met LimAI palette (see DESIGN.md STAP 3)
4. Kopieer apu-composite.webp naar site/public/biomes/apu.webp
5. Basic page.tsx met <BiomeSection> component
6. F6 scrim gradient CSS overlay (linear-gradient spec from DESIGN.md)
7. Typography test: Fraunces titel + Inter tagline
8. Vercel deploy leeg project voor mobile-testing baseline

## DESIGN.md flags — verify in Fase 1

### Niet-meer-actueel om te checken:
- Pacifico architecture: if DESIGN.md describes 3-layer composite, update to 4-layer (sky-subject + plateau + boats SEPARATE + framing-waves). Boats parallax-independence.
- Paracas rol: verify all references updated from "Pricing" to "Proces" (pricing merged into Yungas/Diensten).
- T4 authored drama: verify Selva→Paracas particle transition (leaves→dust metamorphose) still feasible given actual Selva v7 assets.

### Missing / to-be-decided in Fase 1:
- site/ subfolder structure (new, see repo structure below)
- Biome → subpage exact routes (Dutch naming: /over-ons, /diensten, /cases, /proces, /contact)
- F6 gradient as Tailwind utility vs inline style
- Composite path convention (site/public/biomes/ vs symlink to public/Backdrops/)

### Deferred to Fase 2+:
- Lenis smooth-scroll timing values
- Mobile-responsive strategy (composites are 1024x768 landscape)
- Animation timing per transition T1-T5 (durations not locked)

## Repo structure post-Fase-1
```
limai/
├── site/                   ← Next.js project (new in Fase 1)
│   ├── app/
│   ├── components/
│   ├── public/biomes/      ← copies van asset composites
│   └── package.json
├── scripts/                ← Python pipeline (stays)
├── public/Backdrops/       ← asset source-of-truth
├── docs/                   ← design + status + learnings
│   └── learnings/          ← 9 pattern files (2 existing + 7 new)
└── prompts/                ← Flux prompts archive
```

## Immediate next action
Open fresh Claude chat, start with fase-1 start-prompt (see below). Do NOT continue in the Pacifico chat — context-type differs, fresh session for Fase 1 dev-work.
